---
title: LeetCode 2953 | 分治+滑动窗口巧解“统计完全子字符串”
date: 2025-09-21 01:15:12
updated: 2025-09-21 01:15:12
comments: true
tags:
  - 字符串
  - 滑动窗口
  - 哈希表
categories:
  - 算法题解
  - 滑动窗口与双指针
  - 定长滑动窗口
permalink: posts/leetcode-2953/
---
{% centerquote %}
LeetCode 第 2953 题：[统计完全子字符串](https://leetcode.cn/problems/count-complete-substrings/description/)。
这道题巧妙地将“分治思想”和“滑动窗口”结合在了一起。通过识别问题中的强约束条件来分解问题，是解决复杂问题的关键一步。
{% endcenterquote %}
<!--more-->
### 问题解读

我们来仔细分析一下题目中的“完全子字符串”需要满足的两个条件：

1.  **频率条件**：子字符串 `s` 中的**每个字符**，都恰好出现 `k` 次。
2.  **相邻条件**：子字符串 `s` 中，任意**两个相邻字符**在字母表中的位置之差**至多为 2**。

例如，`word = "igigee"`, `k = 2`。
子串 `"igig"` 满足相邻条件 (`'i'` 和 `'g'` 相差 2)，也满足频率条件（`'i'` 和 `'g'` 均出现 2 次），所以它是一个完全子字符串。
子串 `"igigee"` 中，`'g'` 和 `'e'` 相差 2，`'e'` 和 `'e'` 相差 0，满足相邻条件。但频率不对（`'i'` 出现 2 次, `'g'` 出现 2 次, `'e'` 出现 2 次），因此它也是一个完全子字符串。

而对于 `word = "abacaba", k = 1`，子串 `"abac"` 不满足相邻条件，因为 `'a'` 和 `'c'` 的位置差是 2，但它们不相邻，相邻的是`'b'`和`'a'`，`'a'`和`'c'`。但是，在子串`"ac"`中，`'a'`和`'c'`相邻，位置差为2，满足条件。

### 核心思路：分治 + 滑动窗口

直接在一个循环里处理两个条件会非常复杂。我们注意到，“相邻条件”是一个非常强的局部约束。如果 `word` 中存在两个相邻字符 `word[i]` 和 `word[i-1]`，它们的字母表位置差大于 2，那么任何**跨越** `i-1` 和 `i` 这两个位置的子字符串都**不可能**是完全子字符串。

这个特性给了我们一个绝佳的“分治”机会！

#### 分割字符串（Divide）

我们可以根据“相邻条件”将原字符串 `word` 切分成若干个独立的、更小的“有效子段”。在每个有效子段内部，任意相邻字符的位置差都 `<= 2`。这样，我们只需要在这些独立的子段内部寻找满足“频率条件”的子字符串即可。

例如，如果 `word = "aaabccdeeff", k = 2`，字符 `'c'` 和 `'d'` 的位置差为1，但`'c'`和`'e'`位置差为2，而`'b'` 和 `'c'` 的位置差为 1。假设 `word = "aaabcxyz"`，`'c'` 和 `'x'` 的位置差远大于 2，所以我们可以在它们之间切一刀，将问题分解为在 `"aaabc"` 和 `"xyz"` 两个子串中分别求解。

#### 在子段内求解（Conquer）

对于每一个被分割出的有效子段，我们现在只需要解决一个简化版的问题：**找出所有满足“每个字符恰好出现 k 次”的子字符串**。

这正是滑动窗口的用武之地。但这里有一个挑战：完全子字符串的长度不是固定的。

*   如果子字符串包含 1 种不同字符，其长度必须是 `1 * k`。
*   如果子字符串包含 2 种不同字符，其长度必须是 `2 * k`。
*   ...
*   如果子字符串包含 `d` 种不同字符，其长度必须是 `d * k`。

因此，我们可以遍历所有可能的“不同字符数”（从 1 到 26），对于每一种情况，我们使用一个**定长滑动窗口**来寻找答案。

### 算法详解

1.  **分割阶段**:
    *   遍历整个字符串 `word`。使用一个指针 `i` 标记当前有效子段的开始。
    *   使用另一个指针 `j` 从 `i` 开始向后寻找，直到 `abs(ord(word[j]) - ord(word[j+1])) > 2` 或到达字符串末尾。
    *   此时，`word[i:j+1]` 就是一个内部满足“相邻条件”的有效子段。
    *   将这个子段交给下一步处理，然后将 `i` 更新为 `j+1`，开始寻找下一个有效子段。

2.  **处理子段阶段**:
    *   对于上一步得到的每个有效子段 `s`，我们设计一个函数来计算其中的完全子字符串数量。
    *   在这个函数内部，我们遍历“不同字符数 `d`”，从 1 到 26。
    *   对于每个 `d`，我们计算出对应的窗口长度 `window_len = d * k`。如果 `window_len` 大于当前子段 `s` 的长度，就没有必要继续了。
    *   使用一个长度为 `window_len` 的滑动窗口遍历子段 `s`。
    *   在窗口滑动的每一步，我们都检查窗口内的子串是否满足：
        *   恰好包含 `d` 种不同的字符。
        *   每种字符的出现次数都为 `k`。
    *   如果满足，则结果计数加一。

### Python 代码实现

```python
from collections import Counter

class Solution:
    def countCompleteSubstrings(self, word: str, k: int) -> int:
        n = len(word)
        res = 0
        i = 0
        
        # 步骤 1: 分割字符串
        # 根据相邻字符差异 > 2 的地方作为切分点
        while i < n:
            j = i
            # 找到当前有效子段的终点
            while j + 1 < n and abs(ord(word[j]) - ord(word[j+1])) <= 2:
                j += 1
            
            # 对子串 word[i:j+1] 进行处理
            res += self._count_in_segment(word[i:j+1], k)
            
            # 移动到下一个子段的起点
            i = j + 1
            
        return res

    def _count_in_segment(self, s: str, k: int) -> int:
        """
        在已满足“相邻条件”的子段 s 中，计算满足“频率条件”的完全子字符串数量。
        """
        count = 0
        # 步骤 2: 遍历所有可能的窗口长度
        # distinct_chars 是窗口内不同字符的数量，从 1 到 26
        for distinct_chars in range(1, 27):
            window_len = distinct_chars * k
            if window_len > len(s):
                # 窗口长度已超过子段长度，后续更长的窗口也无意义
                break
            
            # 使用固定长度为 window_len 的滑动窗口
            w_cnt = Counter(s[:window_len])
            
            # 检查第一个窗口
            if self._is_complete(w_cnt, k):
                count += 1

            # 开始滑动
            for r in range(window_len, len(s)):
                l = r - window_len
                
                # 窗口右侧加入一个新字符
                w_cnt[s[r]] += 1
                
                # 窗口左侧移出一个旧字符
                w_cnt[s[l]] -= 1
                if w_cnt[s[l]] == 0:
                    del w_cnt[s[l]]
                
                # 检查当前窗口是否为完全子字符串
                if self._is_complete(w_cnt, k):
                    count += 1
        
        return count

    def _is_complete(self, counter: Counter, k: int) -> bool:
        """
        辅助函数：检查一个计数器中的所有字符频率是否都严格为 k。
        """
        if not counter:
            return False
        # 遍历计数器中所有字符的计数值
        for freq in counter.values():
            if freq != k:
                return False
        return True
```

### 复杂度分析

*   **时间复杂度**: O(N)。虽然代码有两层循环，但我们可以这样分析：外层循环负责分割字符串，对 `word` 的每个字符访问一次，是 O(N)。内层的 `_count_in_segment` 函数，其循环 `for distinct_chars in range(1, 27)` 最多执行 26 次。在每次循环中，滑动窗口会线性扫描一次子段。由于所有子段的长度之和为 N，所以总的时间复杂度是 O(26 * N)，即 O(N)。
*   **空间复杂度**: O(K)，其中 K 是字符集的大小（这里是 26）。`Counter` 最多存储 26 个字符的频率，因此空间复杂度是 O(1)。

### 总结

本题是“困难”题中的一股清流，它展示了如何通过分析问题约束来简化问题。
关键的收获是：
1.  **识别强约束**：题目中的“相邻条件”是比“频率条件”更强的局部约束，它可以作为我们分割问题的依据。
2.  **分而治之**：将一个复杂问题分解为多个独立的、更简单的子问题。
3.  **模式识别**：在子问题中，我们识别出了一个“变种”的滑动窗口问题——窗口长度可变但有规律，通过遍历所有可能的长度来解决。

